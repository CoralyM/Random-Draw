export default function Footer({name}) {
    return (
        <div className="w-screen bg-red text-white text-center py-5">
            Made with 🫶 by <strong>{name}</strong>
        </div>
    )
}